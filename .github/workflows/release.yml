name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - run: go vet ./...

      - run: go test ./...

      - name: Cross-compile
        env:
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          mkdir -p dist
          build() { CGO_ENABLED=0 GOOS="$1" GOARCH="$2" \
            go build -trimpath -ldflags "-s -w -X main.version=$TAG" -o "dist/$3" .; }
          build darwin  arm64  mano-darwin-arm64
          build darwin  amd64  mano-darwin-amd64
          build linux   amd64  mano-linux-amd64
          build linux   arm64  mano-linux-arm64
          build windows amd64  mano-windows-amd64.exe
          (cd dist && sha256sum mano-* > checksums.txt)

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          gh release create "$TAG" \
            dist/mano-darwin-arm64 \
            dist/mano-darwin-amd64 \
            dist/mano-linux-amd64 \
            dist/mano-linux-arm64 \
            dist/mano-windows-amd64.exe \
            dist/checksums.txt \
            --title "$TAG" \
            --generate-notes
